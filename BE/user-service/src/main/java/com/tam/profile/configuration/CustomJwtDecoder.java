package com.tam.profile.configuration;

// Removed — common-lib's tam.common.config.CustomJwtDecoder is used instead.
// That decoder calls identity-service /auth/introspect and caches results in Redis.
