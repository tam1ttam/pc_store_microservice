spring:
  application:
    name: saga-orchestrator-service

server:
  servlet:
    context-path: /saga-orchestrator-service
  port: 6058

grpc:
  client:
    user-service:
      address: ${USER_SERVICE_GRPC_ADDRESS:static://localhost:6173}
      negotiationType: plaintext
  server:
    port: ${SAGA_ORCHESTRATOR_GRPC_PORT:6158}
  user-service:
    deadline-ms: ${USER_SERVICE_GRPC_DEADLINE_MS:3000}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:6059/eureka/}
  instance:
    prefer-ip-address: true

