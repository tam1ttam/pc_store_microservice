apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      enableServiceLinks: false
      imagePullSecrets:
        - name: gitlab-registry
      containers:
        - name: product-service
          image: registry.gitlab.com/tam1ttam-group/pc_store_microservice/product-service:latest
          ports:
            - containerPort: 6067
            - containerPort: 6167
          env:
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx256m"
            - name: PRODUCT_SERVICE_PORT
              value: "6067"
          envFrom:
            - secretRef:
                name: app-secrets
            - configMapRef:
                name: app-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 6067
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 6067
            initialDelaySeconds: 120
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: app
spec:
  selector:
    app: product-service
  ports:
    - name: http
      port: 6067
      targetPort: 6067
    - name: grpc
      port: 6167
      targetPort: 6167
