package com.tam.profile.configuration;

// Removed — common-lib's tam.common.config.BaseSecurityConfig handles security.
// Public endpoints are configured via app.security.permit-all-endpoints in application.yaml.
