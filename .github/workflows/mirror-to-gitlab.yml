name: Mirror to GitLab

on:
  push:
    branches:
      - prod

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to GitLab
        run: |
          git remote add gitlab https://oauth2:${{ secrets.GITLAB_TOKEN }}@gitlab.com/tam1ttam-group/pc_store_microservice.git
          git push gitlab HEAD:prod --force
